# SAAF Mumbai — Claude Code Instructions

**The full, canonical instructions for this project are in [`AGENTS.md`](./AGENTS.md). Read that file first and follow it exactly.** It applies to all AI assistants working in this repo (Claude Code, Codex, Antigravity).

## The two rules you must never break

1. **Never push to `main`.** Every change goes through a branch and a Pull Request. (GitHub will reject direct pushes to `main`.)
2. **No user accounts, no tracking, no user data.** This is a hard product constraint — see `AGENTS.md` and `docs/PRD-v1.2.docx`.

Everything else — the per-feature workflow, folder structure, coding rules — is in `AGENTS.md`.
